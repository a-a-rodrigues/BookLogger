<Page x:Class="BookLogger.Desktop.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:BookLogger.Desktop"
      mc:Ignorable="d"
      Title="LoginView" Height="450" Width="800">

    <Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="65*" />
        <ColumnDefinition Width="35*" />
    </Grid.ColumnDefinitions>

    <!-- Left image section -->
    <Border Grid.Column="0" Background="LightGray">
        <Image Source="/Views/logo.JPG" Stretch="Uniform" HorizontalAlignment="Left"/>
    </Border>

    <!-- Right login section -->
    <Border Grid.Column="1" Padding="30" Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right">
            <TextBlock Text="Username" Margin="5,0,0,5"/>
            <TextBox Name="UsernameBox" Margin="5,0,0,10" Width="200"/>

            <TextBlock Text="Password" Margin="5,0,0,5"/>
            <PasswordBox Name="PasswordBox" Margin="5,0,0,20" Width="200"/>

            <Button Content="Login" Margin="0,0,0,10" Width="200"/>
            <Button Content="Create Account" Width="200"/>
        </StackPanel>
    </Border>
</Grid>

</Page>
