<Page x:Class="BookLogger.Desktop.Views.UserCreationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:BookLogger.Desktop.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="UserCreationView" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <!-- Left panel for logo -->
        <Border Grid.Column="0" Background="LightGray">
            <Image Source="/Views/logo.JPG" Stretch="Uniform"/>
        </Border>

        <!-- Right panel for registration -->
        <Border Grid.Column="1" Padding="30" Background="White">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
                <!-- Username -->
                <TextBlock Text="Username" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Email -->
                <TextBlock Text="Email" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Password -->
                <TextBlock Text="Password" Margin="0,0,0,5"/>
                <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,10"/>

                <!-- Confirm Password -->
                <TextBlock Text="Confirm Password" Margin="0,0,0,5"/>
                <PasswordBox PasswordChanged="ConfirmPasswordBox_PasswordChanged" Margin="0,0,0,20"/>

                <!-- Buttons -->
                <Button Content="Back to Login" Command="{Binding BackToLoginCommand}" Margin="0,0,0,10"/>
                <Button Content="Register" Command="{Binding RegisterCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
